"""Domain services that compose the :mod:`slurmhub.slurm` command layer.

Job aggregation, queue statistics, snapshot caching, and log-path resolution:
the higher-level logic the TUI, GUI, and persistence layers build on.
"""
