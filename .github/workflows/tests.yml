name: Tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pytest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install Qt system libraries (for headless PySide6 tests)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libegl1 libgl1 libxkbcommon0 libdbus-1-3

      - name: Install project + dev deps
        run: uv sync --group dev --python ${{ matrix.python-version }}

      - name: Run pytest
        # COLUMNS / LINES / TERM keep Textual's pilot deterministic; the Qt GUI
        # tests render headlessly via the offscreen platform plugin.
        env:
          COLUMNS: "120"
          LINES: "36"
          TERM: "xterm-256color"
          QT_QPA_PLATFORM: "offscreen"
        run: uv run --python ${{ matrix.python-version }} pytest -v --tb=short
